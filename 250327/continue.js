var value = 0;

while (value < 10) {
  value++;

  // if (value % 2 == 0) {
  //     continue;
  // }

  // 삼항연산자
  // break나 continue를 사용할 수 없다
  // (value % 2 == 0)? continue : console.log("value : " + value);
}
console.log("value : " + value);
